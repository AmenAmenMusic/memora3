<!DOCTYPE html>
<html lang="en" class='vh-100'>

<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "Memora2" %></title>
  <%= csrf_meta_tags %>

  <%= stylesheet_link_tag "application", :media => "all" %>



  <%= javascript_include_tag "application" %>

  <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
  <!--[if lt IE 9]>
    <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.2/html5shiv.min.js" type="text/javascript"></script>
    <![endif]-->
</head>

<body class='' >
  <nav class=" navbar navbar-expand-sm navbar-dark bg-dark">
    <a class="navbar-brand" href="/">
      <img src="<%= asset_path 'apple-touch-icon-114x114-precomposed.png'%>" class="" height="40" />
    </a>
    <%if etudiant_signed_in?%>
    <ul class="navbar-nav ml-auto">
      <li class="nav-item active">
      <%= link_to 'se deconnecter', destroy_etudiant_session_path, class:"btn btn-danger", method: :delete %>
      </li>
    </ul>
    </div>
    <% end %>

     <%if maitre_signed_in?%>
     
     <ul class="navbar-nav ml-auto">
     
 
    
      <li class="nav-item active">
        <%= link_to 'se deconnecter', destroy_maitre_session_path, class:"btn btn-danger", method: :delete %>
      </li>
    </ul>
    <% end %>
  </nav>

  
  



<%  #d-flex flex-column  %>

  <div class="container mt-5 h-100  justify-content-center align-items-center">

<%if notice %>
<%if notice=="Déconnexion réussie." %>
<p class="alert alert-danger h5"  role="alert" data-dismiss="alert"><%= notice %></p>
<% else %>
<%if notice=="Connecté avec succès." %>
<p class="alert alert-success h5"  role="alert" data-dismiss="alert"><%= notice %></p>
<% else %>
  <p class="alert alert-primary h5"  role="alert" data-dismiss="alert"><%= notice %></p>
  <% end %>
  <% end %>
  <% end %>
        <%if alert %>
          <p class="alert alert-danger h5" role="alert" data-dismiss="alert"><%= alert %></p>
        <% end %>
  


    <%= yield %>
  </div>

  <footer class="text-center">
  </footer>

</body>

</html>